# Parallel K-Means (C++)

Part of my learning series, the project implements k-means with support for parallelization.

We also do benchmarking to test how would a non parallel version perform.


### Testing
cmake ..
make
./kmeans_app